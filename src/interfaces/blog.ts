export interface BlogProps {
    id: number;
    title: string;
    image: string;
    category: string;
}

export interface BlogCardProps {
    title: string,
    image: string,
    category: string
}